# define, which port the server should listen (defaults to 80)
SERVER_PORT={SERVER_PORT}

# docker application name
DOCKER_NAME={DOCKER_NAME}

# node version for this application
NODE_VERSION={NODE_VERSION}

# node environment to start
NODE_ENVIRONMENT={NODE_ENV}